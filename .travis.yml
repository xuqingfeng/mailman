language: go
go:
  - 1.7
  - tip
os:
  - linux
before_install:
  - go get golang.org/x/tools/cmd/cover
  - go get github.com/mattn/goveralls
install:
  - go get -v ./...
script:
  - ./coverage.sh --coveralls